spring:
  data:
    redis:
      url: ${SPRING_REDIS_URL}
  redis:
    url: ${SPRING_REDIS_URL}

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
